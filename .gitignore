*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Python
__pycache__/
*.pyc

# Test recordings (large files)
test_*.wav
test_*.webm
test_*.mp3

# Temp folders
/AwaitingPNG/
/Spectrogram Work/

# BirdNET V3 model — too large for normal git tracking (~68MB). Deploy this
# file separately (upload directly to the server's models/ folder) rather
# than committing it. The labels CSV is small and safe to keep in git.
/models/*.onnx
